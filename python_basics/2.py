#Type casting : Method of converting the datatype into another datatype

# Implicit --> python itself converts the smaller datatype into larger datatype to prevent data loss
a=8.7
b=4
c=a+b
print(c)

#Explicit --> User can typecast the data
x=1
y=4
z=str(x)+str(y)
print(z)